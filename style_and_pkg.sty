\usepackage{sectsty}
\usepackage[utf8]{inputenc} % This defines the font-encoding you prefer to use
\usepackage{fourier}
\pagestyle{headings}
\usepackage{marvosym}
\usepackage[pdftex]{graphicx}
\usepackage[lmargin=25mm,rmargin=25mm,tmargin=27mm,bmargin=30mm]{geometry}
\geometry{b5paper} % Set the page size to be b5 as opposed to the default US Letter
\usepackage{rotating}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage[hyphens]{url}
% \usepackage[numbers]{natbib}
\usepackage{csquotes}
\usepackage[%
  backend=biber      % biber or bibtex
%,style=authoryear    % Alphabeticalsch
 ,style=numeric-comp  % numerical-compressed
 ,sorting=none        % no sorting
 ,sortcites=true      % some other example options ...
 ,block=none
 ,indexing=false
 ,bibencoding=ascii
 ,citereset=none
 ,isbn=true
 ,url=true
 ,doi=true            % prints doi
 ,natbib=true         % if you need natbib functions
]{biblatex}
\addbibresource{master_references.bib}

\usepackage[section]{placeins}
\usepackage[norsk,british]{babel}

\usepackage[dvipsnames]{xcolor}
\usepackage[obeyFinal]{todonotes}
\usepackage{standalone} % To import subdocuments
\usepackage{import} % To import subdocuments
\usepackage{float} % Allows putting an [H] in \begin{figure} to specify the exact location of the figure
\usepackage{wrapfig} % Allows in-line images
\usepackage{gensymb} % symbols
\usepackage{expl3,xparse}
\usepackage[procnames]{listings}
%\usepackage{lstlinebgrd} % Had to comment out in order for the document to compile
\usepackage{multirow}
\usepackage{enumitem}
\usepackage[title]{appendix}
\usepackage{emptypage}
\usepackage{subcaption} % to put figures side by side 

\usepackage{setspace}
\linespread{1.2} %Changed from 1.5
\usepackage[parfill]{parskip}

\usepackage[ruled,vlined,linesnumbered]{algorithm2e} 

\usepackage{etoolbox}

\usepackage[yyyymmdd]{datetime}
\renewcommand{\dateseparator}{--}
\newdateformat{monthyeardate}{%
  \monthname[\THEMONTH], \THEYEAR}

\newcommand{\myparagraph}[1]{\paragraph{#1}\mbox{}\\}
\newcommand{\mysecondparagraph}[1]{\textit{#1}\mbox{}\\}

\renewcommand*{\lstlistingname}{List of Code}

%%=========================================
%     Sorted list
%%=========================================

\usepackage{datatool}% http://ctan.org/pkg/datatool
\newcommand{\sortitem}[2][\relax]{%
  \DTLnewrow{list}% Create a new entry
  \ifx#1\relax
    \DTLnewdbentry{list}{sortlabel}{#2}% Add entry sortlabel (no optional argument)
  \else
    \DTLnewdbentry{list}{sortlabel}{#1}% Add entry sortlabel (optional argument)
  \fi%
  \DTLnewdbentry{list}{description}{#2}% Add entry description
}
\newenvironment{sortedlist}{%
  \DTLifdbexists{list}{\DTLcleardb{list}}{\DTLnewdb{list}}% Create new/discard old list
}{%
  \DTLsort{sortlabel}{list}% Sort list
  \renewcommand\labelitemi{}
  \begin{itemize}%
    \DTLforeach*{list}{\theDesc=description}{%
      \item \theDesc}% Print each item
  \end{itemize}%
}

%%=========================================
%			Remark environment
%%=========================================
\newenvironment{remark}{\par\mbox{}\par\noindent{\bfseries \emph{Remark}\/}:\normalfont}{\par}
\newenvironment{defin}{\par\mbox{}\par\noindent\bfseries \emph{\Pointinghand\ }\normalfont}{\par}
%%=========================================

\setcounter{tocdepth}{2} 
\usepackage[colorlinks=true, pdfstartview=FitV,
linkcolor=blue, citecolor=blue, urlcolor=blue]{hyperref}
\pdfcompresslevel=9

%%=========================================
%				Info boxes
%%=========================================
\usepackage{mdframed}
\newtheorem{mdinfo}{Info}
\usepackage{comment}
\specialcomment{info}%
  {\begin{mdframed}[backgroundcolor=Goldenrod]\begin{mdinfo}}%
  {\end{mdinfo}\end{mdframed}}

\usepackage{ifdraft}
\ifdraft{}{\excludecomment{info}}
%%=========================================
